#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME};
#end
/**
 * // TODO: 2017/12/13
 *
 * Author: ${USER}
 * Created on ${DATE}
 */
public interface ${NAME}Contract {
  abstract class View<P extends Presenter<Entity>, Entity> extends BaseMVPActivity<P> {

  }

  abstract class Presenter<Entity> extends CommonPresenter<View> {

  }
}