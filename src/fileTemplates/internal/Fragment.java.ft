#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME};
#end
/**
 * // TODO:
 *
 * Author: ${USER}
 * Created on ${DATE}
 */
public class ${NAME}Fragment
    extends ${NAME}Contract.View<${NAME}Contract.Presenter<Entity>, Entity> {

  @NonNull
  @Override
  protected ${NAME}Contract.Presenter<Entity> getPresenter() {
    return new ${NAME}Presenter();
  }
}