#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME};
#end
/**
 * // TODO:
 *
 * Author: ${USER}
 * Created on ${DATE}
 */
public class ${NAME}Fragment extends ${NAME}Contract.View<${NAME}Presenter> {

  @NonNull
  @Override
  protected ${NAME}Presenter getPresenter() {
    return new ${NAME}Presenter();
  }
}