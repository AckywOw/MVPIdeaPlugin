#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME};
#end
/**
 * // TODO:
 *
 * Author: ${USER}
 * Created on ${DATE}
 */
public interface ${NAME}Contract {
  abstract class View<P extends Presenter> extends BaseMVPFragment<P> {

  }

  abstract class Presenter<V extends View> extends CommonPresenter<V> {

  }
}